# Output Templates

Active reusable layouts now live in `templates/`.

Legacy output templates are preserved at `_legacy/2026-06-01-pre-superbot/output-templates/`.

Use `outputs/[Brand Name]/` only for final or shareable deliverables.
