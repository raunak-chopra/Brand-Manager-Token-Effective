# Reference Library

Store selected brand references here only when they are useful for future work.

Summarize each useful reference in `../reference-index.md` so future tasks do not need to bulk-load this folder.

Suggested subfolders:

- `approved/`
- `rejected/`
- `competitors/`
- `visuals/`
- `audience/`
